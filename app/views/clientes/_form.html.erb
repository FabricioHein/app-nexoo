
<%= form_for @cliente, html: { class: "form-style-5" } do |f| %>

  <% if @cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
      <% @cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome, "Nome (Ex: Fulano )" %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :email, "Email (Ex: fulano@gmail.com)" %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :telefone , "Telefone (Ex: 9999-9999)"%><br>
    <%= f.text_field :telefone %>
  </div>
  <div class="field">
    <%= f.label :endereco %><br>
    <%= f.text_field :endereco %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
